version: '3.8'

services:
  # Servicio de base de datos
  mariadb:
    image: mariadb:10.6
    container_name: monitoreo-ambiental-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: monitoreo_ambiental
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./node-monitoring/database-schema.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3306:3306"
    networks:
      - monitoreo-network

  # Servicio backend de monitoreo ambiental
  monitoreo-ambiental:
    build:
      context: ./node-monitoring
      dockerfile: Dockerfile
    container_name: monitoreo-ambiental-api
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DB_HOST=mariadb
      - DB_USER=root
      - DB_PASSWORD=admin
      - DB_DATABASE=monitoreo_ambiental
      - IQAIR_API_KEY=19f1c671-c320-4e74-b039-7cc087d4547e
      - WEATHER_API_KEY=tu_api_key_aqui
    ports:
      - "3000:3000"
    depends_on:
      - mariadb
    networks:
      - monitoreo-network

  # Nuevo servicio frontend de monitoreo ambiental
  ui-monitoring:
    build:
      context: ./ui-monitoring
      dockerfile: Dockerfile
    container_name: monitoreo-ambiental-ui
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - monitoreo-ambiental
    networks:
      - monitoreo-network

  # Servicio para visualización de datos
  grafana:
    image: grafana/grafana:latest
    container_name: monitoreo-ambiental-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - mariadb
    networks:
      - monitoreo-network

volumes:
  mariadb_data:
  grafana_data:

networks:
  monitoreo-network:
    driver: bridge